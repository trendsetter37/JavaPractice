/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package headfirstjava;

/**
 *
 * @author javis
 */
//Prep code stage
public class SimpleDotCom {
    // declare dotcom array

    int[] dotComLocation = new int[3];
    int numberOfHits = 0;

    // constructor to set locations from object instance input
    public SimpleDotCom() {
        
     //   if (a != b && b!=c && c!=a && a <)
        
        
    }

    //check the guess of the player
    public boolean checkGuess(String userGuess) {
    
    return false;
    }

//get user guess as a string parameter
//convert user guess to an int
        /**
         *
         * and check against locations
         *
         *
         *
         */
    }

    //set locatioin of dotcom
    public void setLocationCells(int[] cellLocation) {

        /**
         * get initial cell locations as the method parameter and set that to
         * the instance array dotComLocation
         */
    }

    //REPEAT
    /**
     * compare guess to dotCom location if (guess == location){ ++ numberOfHits
     * variable continue until noh == 3 then report a sunken ship and provide
     * rating.
     *
     * }
     */
}
